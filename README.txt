api-tests

Use

1. Import as maven project
2. Update the maven project
2. Run following command to run the test
	i. mvn clean test
	
3. Run following command to generate the allure report
	i. allure serve <path of the allure-results folder